# The same mounts as runtime_config.toml, backed by SQLite database files
# (paths resolve against this file's directory).
[filesystem.data]
type = "sqlite"
path = "data.db"
writable = true

[filesystem.frozen]
type = "sqlite"
path = "frozen.db"
